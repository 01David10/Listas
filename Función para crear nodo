// funcion que crea un nodo con un dato (x) y una liga (Nodo *enlace) que recibe
Nodo *Crearnodo (int x, Nodo *enlace)
{
  // crear nodo
	Nodo *p;   
  
  // asignacion de memoria
	p = (nodo*)malloc(sizeof(nodo));
  
  p -> dato = x;
  p -> siguiente = enlace;
  return p;
}
